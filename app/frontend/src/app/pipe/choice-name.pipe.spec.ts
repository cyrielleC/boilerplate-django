import { ChoiceNamePipe } from './choice-name.pipe';

describe('ChoiceNamePipe', () => {
  it('create an instance', () => {
    const pipe = new ChoiceNamePipe();
    expect(pipe).toBeTruthy();
  });
});
