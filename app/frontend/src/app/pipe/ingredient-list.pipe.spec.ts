import { IngredientListPipe } from './ingredient-list.pipe';

describe('IngredientListPipe', () => {
  it('create an instance', () => {
    const pipe = new IngredientListPipe();
    expect(pipe).toBeTruthy();
  });
});
